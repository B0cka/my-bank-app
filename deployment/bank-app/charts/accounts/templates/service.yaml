apiVersion: v1
kind: Service
metadata:

  name: accounts-service
spec:
  type: ClusterIP

  selector:
    app: accounts-service

  ports:
    - name: http
      protocol: TCP
      port: 8090
      targetPort: 8090