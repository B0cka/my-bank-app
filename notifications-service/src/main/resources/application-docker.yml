spring:
  application:
    name: notifications-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${APP_SECURITY_OAUTH2_ISSUER_URI}

server:
  port: 9900

management:
  endpoints:
    web:
      exposure:
        include: health,info