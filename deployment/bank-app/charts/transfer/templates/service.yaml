apiVersion: v1
kind: Service
metadata:
  name: transfer-service
spec:
  type: ClusterIP
  selector:
    app: transfer-service
  ports:
    - name: http
      protocol: TCP
      port: 9800
      targetPort: 9800