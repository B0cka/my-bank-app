apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: ClusterIP
  selector:
    app: notifications-service
  ports:
    - name: http
      protocol: TCP
      port: 9900
      targetPort: 9900