apiVersion: v1
kind: Service
metadata:

  name: cash-service
spec:
  type: NodePort

  selector:
    app: cash-service

  ports:
    - name: http
      protocol: TCP
      port: 8900
      targetPort: 8900
      nodePort: 30092